// // src/utils/tauri.ts
// export const tauriInvoke = async <T = any>(cmd: string, args?: Record<string, any>): Promise<T> => {
//   const { invoke } = await import('@tauri-apps/api/core')
//   return invoke<T>(cmd, args)
// }
